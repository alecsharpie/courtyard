# Manager decisions

One line per manager pass. This is the record of what the loop *decided*, as
opposed to `LEDGER.md`, which records what it *did*. Keeping them apart is the
point: when a run goes wrong, the question is almost never "what did the workers
build" — it is "why did anyone think that was the right next thing", and the
previous loop had nowhere that answer was written down.

Format (parsed by `build-stats.mjs`, so keep the shape):

```
- <YYYY-MM-DD> planned from #<N>, rung <R> — <signals seen; one sentence of why this batch>
```

---

- 2026-08-03 planned from #0, rung 2 — cold start, no runlog, laws 16/60, budget 19.6/46 KB OK; the town has 31 iterations of nouns and one `connect` in its whole history, so the opening batch wires existing systems together (dead `bellUntil`, no clouds at all, no agent-to-agent code at all, east half starved of spawn budget) rather than adding an eighth kind of thing, led by a small brief to shake out the gates before the risky one.
- 2026-08-03 planned from #5, rung 2 — 4/4 workers shipped, 0 reverts, diffs 46/88/86/80 lines, no stall signal, laws distilled 16→21 (three PRNG-churn laws merged into one, two time-compression laws into one), budget 38.3/46 KB OK; held rung 2 a second time because three domains and five of seven kinds are still untouched by the loop, and aimed the batch at the town's characteristic defect — systems shipped with too few readers (cloudCover has one, updateBoat none) — after killing two plausible briefs against the source, and flagged c12: the runlog records no cost or duration for any worker row, so the metric that killed the previous loop is currently unmeasurable.
- 2026-08-03 planned from #10, rung 4 — 9/9 workers shipped, 0 reverts, diffs 46-177L, no stall signal, but I found WHY every worker row logs secs=0/$0: the worker writes its own runlog row, so the runner's call with --elapsed/--raw/--pre-blob hits the "already recorded" guard at runlog.mjs:55 and the only row with real numbers is discarded every iteration (c12+c14+c20 were one bug; real durations are sitting in the runner log, flat at 558-1816s). Climbed to rung 4 and spent c1 (the world pins at maturity 1 by real minute 15) deliberately AGAINST pass #5's "wait until a batch comes back thin" — Solvista only climbed once it was already reverting 80% of the time, and a season is not a thing you attempt from there; climb while nine iterations in a row have shipped clean. Also found the town has been clickable with six distinct responses since before the loop began and the coverage grid's empty interaction column is a fact about the LOOP, not the town — so the rung-5 brief is discoverability, not interaction. Laws 21->28 (nine promoted from #6-#10, minus two merges: the rate-vs-existence lesson had bought two separate laws for one three-times-paid lesson, and three probe-hygiene laws became one), cues 16->8, inventory compressed back to nouns, worker budget DOWN 44.7->42.6 KB while the laws file grew.
- 2026-08-03 planned from #15, rung 2 — 13/14 workers shipped, 0 reverts, no stall signal, and cost is finally MEASURED and FALLING (1835/1289/1046/909/726 s, $5.27/$6.36/$3.84/$4.59/$2.95) which is the inverse of the signature that killed Solvista; the one watch is srcLines 103/101/56/41 with two attempts at #15, so this batch spends deliberately more than the last. Held rung 2 rather than climbing because pass #10's climb to rung 4 is only half done: the year shipped at #12 and has THREE readers — day length, light, the courtyard beds — while thirteen trees stand in full summer leaf in January, the whole working day (kioskOpen 7.5-18.5, marketActive 8-17, marketRaise from 6.90, the sweeper at 5.00) is still nailed to the clock although b11 moved sunrise to 7.00 in winter and 4.00 in summer, and the population breathes 15% because the caps compress daylight to 0.25+0.75d. You do not climb on top of an unread system. Balance: sky had 3 of the last 5 and courtyard 2 of the last 3 while the entire east half went ten iterations untouched, so street/lane/life/plaza get one each and sky gets only the harness job. Extended the menu with a `harness` kind (rung-3 move, small) so instrument work stops being filed against a town domain — the same disease as c23, my own footprints counted as evidence. Ruled c23 myself: streak over kind==='worker' rows only. Curation was the harder half — the budget had been OVER since #13, so three workers ran ~10% above the cap: cues 20->7, inventory back to nouns, two of three watch notes deleted as prose restatements of laws, and LAWS.md promoted four laws from #12-#15, hit 11.8/12 KB, and had to be distilled back to 9.4 by merging five and cutting one outright. The binding cap on that file is BYTES not laws (32/60 and over) and that is now written in its header. Worker budget 50.8 -> 45.7 KB.
- 2026-08-04 planned from #20, rung 2 — 17/19 workers shipped (both no-ships briefed as harness), 0 reverts, no stall signal, cost flat at $4.2-4.7 with two spikes that are the run's two largest jobs, srcLines recovered 198/58/73/66 from the slide I flagged at #15; the one real fault was mine — context-budget printed OVER at #17, #19 and #20 and three consecutive workers each wrote the same cue about a file only I can touch, which is Solvista's terminal pattern in miniature, so curation came first and was done structurally rather than by trimming: ledger rotated 13->8, the last-3 read cut 15.3->9.1 KB by condensing #18-20 into LEDGER-archive.md verbatim, a ~3.5 KB per-entry cap written into the ledger template with the reason beside it, inventory back to nouns for the third time, cues 19->5, LAWS.md allowed to 11.4 KB then cut to 11.0 (dropped the ticker-queue law, whose content the inventory now carries exactly) with four of five promotions merged into existing laws rather than stacked, total 52.0 -> 43.3 KB. Held rung 2 to close the year spine: #12 gave the world a year, pass #15 counted three readers, there are now six, and exactly two systems still run on a dead ramp — the weather (stepClouds on richness(), which pinned in real minute fifteen; the top cue since #10 and declined by me twice) and the river (one iteration in twelve, flat boat rate and unnamed inline flow streaks). b21 is sequenced after b20 on purpose because b20 turns overcast from rare to most of a winter and only then is c7 worth paying for; b23 is deliberate seed for a rung-5 climb at pass #25 so the observer move is not made from a cold start; b24 is explicitly permitted to come back as a measurement with no source change.
- 2026-08-04 planned from #25, rung 5 — 20/24 workers shipped, 0 reverts, 0 briefs rejected, no stall signal, and cost is FALLING across the last five ($8.30/$6.55/$6.11/$6.49/$4.50) which is the inverse of Solvista's signature; #25's 35 source lines is the smallest since #15 and is the brief working rather than the loop shrinking — it was a measure-first brief, three of its premises came back false, and four lines moved ripe==0 from 48.3% of the year to 13.1%. CLIMBED TO RUNG 5 as pass #20 said I would if b20-b24 landed, and all five did. The case is one number: the year is ~24 real minutes, nine systems now read it, and a thirty-second viewer sees 2% of it while the sill has said "Day 12 · Afternoon" since before the loop and has never named the season — fourteen iterations of seasonal work addressed to an observer with no way to reach it. b25 names the season cheaply and is sequenced first because b27, the batch bet, needs a viewer who can already read the year before it gives them a way to travel through it; b27 is genuinely hard because every slow scalar here is rate-capped so "it never steps" is a guarantee and six systems carry hysteresis, so a season jump is a pop unless the transition is honest. b26 puts the other observer brief in the starved domain — 64% of the frame answers a click since #24 and nothing says WHAT you are pointing at, with eleven species and thirteen trees unnameable. b28 and b29 are deliberately not observer work, because five briefs on one theme is a monoculture: the market is the last flat system in the town (three stalls, identical in February and August, while the allotments next door ripen and are picked on a full year and gardeners carry baskets that go nowhere), and c11 has been open since #5 waiting on routeToExit(), which now exists. FOUND A BUG IN MY OWN INSTRUMENT: state.mjs computed each domain's last-touched iteration by stripping non-digits, so pre-loop tags pre28/pre29/pre31 parsed as iterations 28/29/31 in a run that has reached 25 — the balance grid reported Courtyard as the freshest domain when it is the most starved by seven, and I have planned three passes against it. Fixed, and it now prints how many iterations back each domain is. Curation was half the pass and the repeat failure was again mine: context-budget printed OVER at #22, #24 and #25, five workers across two passes, because my pass-#20 fix was an advisory 3.5 KB cap in a template that the next three entries (4.3/5.7/4.9 KB) ignored — an advisory cap is a comment, so rotate-ledger.mjs now measures the last three entries BY NAME and will not be quiet about one over the cap, which is a script I run every pass. Eleven law promotions from #21-#25 absorbed into 25 laws (was 27): eight went in as merges — presence-vs-rate into time-compression, the ten-seed rule into R()-reshuffle, the neutrality algebra into the slow-scalar law, footprint-off-the-grid into one-predicate-one-definition, the census ladder into pin-the-instant, grain-matching into the ceiling law — and only three are new, all about slow things, of which hysteresis is the loop generalising rather than repeating (#21 found it in a rate-capped scalar, #23 re-derived it independently through a long-lived object). Paid for them by folding the flat-peak law into the slow-scalar law, read-probes-first into probe-rewind, merging the two rate-of-zero laws, deduplicating three invariants that SKILL.md and LAWS.md both stated verbatim, and compressing the register throughout — the essayistic style was itself the cost. Ledger last-3 14.8 -> 8.6 KB with full text archived, per-entry cap tightened 3.5 -> 3.0 KB, cues 13 -> 6 (one promoted to b29, six closed including two superseded by the laws they became), inventory back to nouns for the fourth time with the measurements stripped out. Worker budget 55.0 -> 45.3 KB OK.
- 2026-08-04 planned from #30, rung 2 — 18/20 of the last workers shipped (both no-ships briefed as harness), 0 reverts, 0 briefs rejected, src moved 10/10, cost FLAT across the last ten (mean $6.26 vs $6.32 for the ten before), no stall signal; the run is in its longest clean stretch and the only fault is mine. HELD RUNG 2 rather than climbing: the ladder says stay low while work lands, and pass #25's rung-5 climb succeeded, so what it needs is finishing, not repeating. THE REPEAT FAULT, third pass running: context-budget opened OVER at #28 (47.1), #29 (48.4) and #30 (49.8) — I left pass #25 at 45.3 KB with 0.7 KB of headroom and it re-grew at 1.3 KB per iteration. This is not a distillation failure and I have now stopped treating it as one. I had capped and ENFORCED the two files I was watching (LAWS.md 12 KB, ledger entries 3 KB) and the growth relocated to the one file with no cap at all: state.json's inventory had reached 11.4 KB, LARGER THAN LAWS.MD, because every landed iteration adds a system to it by construction and nothing ever said what a system costs. That is the Solvista failure's exact shape — cap one file and the growth moves — which is what the budget script was written to prevent, and it caught the symptom three times while the cause sat in the file the script only measured in aggregate. So the fix this pass is caps, not prose: rotate-ledger.mjs now enforces 300 B per inventory entry, 8 open cues, 900 B per single law (a law past a paragraph is two laws pretending to be one, and it reaches the file cap without anybody deciding to spend it), and the ledger entry cap tightened 3.0 -> 2.5 KB — each one NAMING its offenders, which is the only thing that worked last time. Inventory 11.4 -> 8.5 KB by stripping the measurements its own header has said belong in the ledger since the day it was written; cues 15 -> 6 (three promoted to briefs, one promoted to a law, three closed, two merged, and the duplicate-id fault the worker raised as c59 fixed in place rather than briefed, since I own that file); ledger last-3 9.5 -> 6.9 KB with #29 and #30 archived verbatim first, and the template now says a promoted Law/Cue line is CUT from the entry, because it is being read three times in a file that exists to be read instead of the other two. LAWS.md 12.0 -> 11.5 KB, 26 -> 25 laws, four promotions from #27-#30 absorbed: #29 and #30 merged into the probe-rewind law, and #29 is the good kind of promotion because it says the existing rule was right for the WRONG REASON (not that the sim keeps running — the RENDERER draws from the PRNG, so any host round-trip walks the seeded stream), so the law is now correct rather than merely useful; #27+#28 became one new law, "the sill is DOM and DOM fails quietly", closing a real gap since three of the last four iterations touched the sill and there was no law about it; #30's "a zero is evidence only if you show the test can be non-zero" is the other new one. Paid for by splitting the 1255 B slow-scalar law in two, merging locate-before-you-judge into compare-against-a-control and screenshots-cannot-see-motion into the filmstrip law, and cutting the split-flag law's "fade the largest thing first" clause outright as a duplicate of the filmstrip law. Also recorded #28 in the coverage grid, which had never been entered, so the grid read Sky as 4 back when it was 2. THE BATCH: the rung-5 tools are built and SILENT — #26 named the season, #27 named what is under the pointer, #28 made the season a button that reaches midwinter in 19.6 real seconds, and the only invitation in this town is about clicking the canvas, so the loop shipped a fourth instance of its own thrice-paid advertise-it law the iteration after reading it; b30 closes that instance and the pattern went into WATCH where a brief-writer meets it, because writing the law a fourth time is not the answer. b31 is the bet and the only brief here that can fail: the far bank has had a BANDSTAND since before the loop and nothing has ever happened in it, in the domain 7 iterations back, on a map whose east half has no gathering of any kind — every crowd this town has is transient, and an audience that arrives, stands still together and packs away is a shape the loop has never built. b32 answers the other half of the same finding: nine systems read season() and every one is west of the bridge or in the sky, so the plaza and its fountain are in the exact position the market was in before #29. b33 is a small correctness fix that a shipped feature turned into a falsehood — the opening scatter puts ferns in the allotment block and since #29 they can ride a basket to a market stall. b34 finishes #27 in a grid cell (People x Interaction/UX) that has never been touched. Worker budget 51.0 -> 44.1 KB OK.
- 2026-08-04 planned from #32, rung 4 — 19/20 shipped, 0 reverts, 0 briefs rejected, src moved 10/10, but COSTSPIKE FIRED: last 3 averaged $13.54 vs a $6.11 median. Decomposed it rather than reacting to it: #31 alone was $22.72, 3.7x the median, and its own entry says why — three instrument failures in one iteration (a comment that swallowed a CSS rule, a margin computed with a floor so it could only report bad news, an arrival time that was never assertable); strip it and the last ten average $6.63 vs $6.26 and $5.19 for the two tens before, ~+13% per ten, not Solvista's 2.5x. The true reading is not that the loop is dying but that I have been mis-labelling: the two most expensive iterations in the run are the two most recent and I briefed BOTH at rung 2, and a concert with its own spawn source, own tick and nine claimed slots is not Deepen — so the run was paying the price of climbing without taking the benefit. CLIMBED TO RUNG 4. The world only exists for ~60% of its own clock: all TEN itinerary branches in spawnLaneAgent open with `sun &&` (daylight > 0.3), so after dark every walker falls through to the pass-through leg and stops nowhere, while kioskOpen, marketOpen, bandEnd and eastOpen close on the sun too — night is ~40% of the clock, adds lit windows, fireflies, one cat and two ticker lines, and thirty-two iterations have densified the daylight town without once touching the other half of the day. b35 (Lane x Scale/World, first Scale/World outside Sky, risk high) buys one place that keeps hours after dark and people who go there, measure-the-dark-first; b36 takes the same finding for free — drawWindow decides lit as one hash per building per day above nightF 0.3, so 58% of windows burn dusk to dawn and nobody goes to bed, and 22.00 and 03.00 are currently the same picture. Two night briefs in one batch is the point of a climb: commit to the ground, do not dabble at its edge. b32/b33 carry forward unchanged, b37 promotes c50 (bloomCap is the only STEPPED seasonal term in a file of lerps) after three passes open — a cue that survives three passes is a brief or a deletion. b34 dropped to cue c64, not lost. FOUND MY OWN INSTRUMENT LYING AGAIN: the coverage grid read Sky as 1 back and over-served, but #26/#28/#31 are pure SILL iterations filed under Sky because the sill draws the weather word — added a `sill` domain at rung 3, re-filed those three (left #13/#27, which delivered town-side behaviour), and Sky is 10 back and the most starved domain in the town, with Sky x Polish marked saturated at 6 entries. THE REPEAT FAULT, FOURTH PASS: budget opened OVER at 47.5 KB, 2 iterations after I left it at 44.1 — pass #30 capped inventory entries by BYTE and cues by COUNT, and the growth went into the dimension with no byte cap, cues, 3.5 KB for 9 at ~390 B each, each carrying evidence that belongs in the ledger entry that raised it. Cap one dimension, growth moves next door — three consecutive passes of the same shape on my own watch — so this time every dimension has both caps and rotate-ledger.mjs enforces all of them by name: 8 domains x 8 entries x 300 B, 8 cues x 250 B, 4 watch x 420 B, multiplying to a CEILING of 45.0 KB rather than a number the file happens to sit at. 47.5 -> 45.2 KB. Also dropped two inventory entries that stated an ABSENCE (the bandstand `has never been used`, which #32 built; the fountain `same in February as in August`) — an absence is a cue wearing the wrong hat and goes stale the moment somebody builds it. LAWS 25 -> 26 laws, 11.5 -> 12.0 KB: two new (#31 when a gate fails suspect the instrument first, absorbing the floored-margin and the unassertable-arrival findings; #32 an event its audience must WALK to is bounded at both ends by things that are not the event), paid for by cutting `read the seam, not the file` OUTRIGHT as a verbatim duplicate of SKILL.md's read table (a law that restates the protocol pays rent twice; its only unique fact, the file size, was stale at ~3,000 and is now ~5,300 where the worker actually reads it) and tightening five others. Promoted Law paragraphs replaced in their ledger entries by one-line pointers after archiving #31 verbatim, which brought both over-cap entries back under without losing anything the archive does not hold.
- 2026-08-28 planned from #37, rung 4 — the last two REAL iterations shipped (#33 the evening door, 193 lines, $18; #37 the bloom ramp, 30 lines, $3.44 in 5 min on fable-5), 0 reverts, 0 rejected, but three of the last ten rows (#34–36, plus a manager row at #33) are 1–2 s zero-token rc=1 synthetic launch failures from the 2026-08-28 restart, and each one BURNED a brief because run-loop.sh pops before the worker starts and a non-zero rc just pops the next: b36/b32/b33 were destroyed unexecuted and the dashboard reads 7/10 for a run that shipped 7/7. No stall signal fired because stall.mjs counts them as ordinary failures. HELD RUNG 4 — the #32 climb (the town has no evening) is half-landed: one door exists, every window still switches together — and re-issued the three burned briefs verbatim with a no-prior-attempt note, which is recovery not repetition; b38 at the head is the harness fix (a launch failure must leave the brief claimed and must not count as an iteration; carries c62), b39 promotes c64 (the naming cannot name anything that moves) as the batch's bet. Three of five briefs are in domains 12+ back. Curation: coverage entered for #33/#37 (never recorded), six cues closed/promoted/absorbed (9 -> 3), an id collision fixed (open c61 -> c68), life inventory 9 -> 8 by merging the two own-source crowds, #37 archived verbatim and #33/#37 trimmed 2.9 -> 1.6 KB each, two law promotions paid for by compressing five laws (26 laws, 11.9/12 KB). Worker budget 47.0 -> 43.9 KB OK.
- 2026-08-28 planned from #42, rung 4 — 9/10 of the last real rows shipped, the one no-ship is the harness brief mis-graded by runlog (srcChanged=false is its definition), 0 reverts, 0 rejected, cost on fable-5 falling to a ~$3.4 median with 30–68-line diffs and a probe each — the inverse of the Solvista signature; but state.json was three iterations stale and the worker budget opened OVER twice. HELD RUNG 4 for the third pass because the night climb is still half-landed: windows (#39) and the door (#33) exist and connect to nothing — nobody walks to the door, the east shuts at daylight 0.16, the river at night is noon water under a blue multiply. b43 (river x connect: lamp/moon on the water, one quay stop after dark) and b44 (lane x connect: the concert audience walks to the door; carries c65/c68) finish it; b42 SNOW is the bet (sky x scale, high risk, exactly-zero-at-anchor on the cached ground) because a viewer landing in February cannot tell it is winter from the wide shot; b41 swans get behaviour (life 12 back, pays c73); b40 fixes three instrument lies (night filmstrip is a day strip, harness briefs grade no-ship, crashed workers retire briefs). Life/river/lane are the three most starved and get three of five. Curation: coverage #38/#40/#41/#42 entered; 8 cues closed (5 promoted, 3 no-yield); 4 laws promoted paid for by compressing 6 (27 laws, 12.0/12 KB); ledger 12->8; inventory note 700->230 B, sky 9->8; budget 46.4 -> 43.0 KB OK.
- 2026-08-29 planned from #47, rung 2 — 20/20 of the last real rows shipped, 0 reverts, 0 rejected, selfVerdict == verdict throughout, src moved 8/10, $3.87/iter with 74–95-line diffs and a probe each (cost rising WITH output); the only fault is the worker budget, OVER three iterations running (50.5/46 KB) because three 3.4–3.8 KB entries landed with no manager pass between them. DROPPED RUNG 4 -> 2 on purpose: the night climb that passes #32/#37/#42 held rung 4 for is landed (windows #39, door callers #47, river lamps + night stander #46, snow #45), so the highest-yield move is to compound it, not open a fourth world change. b45 first (sky x deepen: the cached layer relights the whole picture in one frame at winter dusk, 5–9 Δ on HEAD — a defect a viewer sees), b46 snow x desire paths + the sweeper (courtyard, 10 back), b47 the plaza gets the wind (7 back; nothing east of the bridge reads isWindy), b48 the bet: leavers go HOME and light the window they went to (life x connect, cached facade + WINDOWS[] + routeToExit, can fail), b49 the allotments get a winter (street, 6 back; allotRate floors at 0.01 with ripePlots 0). Curation: coverage #43–#47 entered (five stale); c75/c76/c77 promoted, c74/c78/c79 held; three laws promoted (#44 follower-rate new; #46 night cap and #47 pathHours merged INTO the time-compress law; __entities extras, __warp-never-draws, filmstrip --page folded into existing laws) paid for by compressing eight — 28 laws, 12.0/12 KB, none cut; #45–#47 archived verbatim then condensed to 1.8 KB each; inventory 59 -> 46 entries by merging pairs; WATCH: two spent notes replaced (night is built but not wired to the day; the wind is read nowhere east). Budget 50.5 -> 43.5 KB OK.
- 2026-08-29 planned from #52, rung 2 — 19/20 of the last real rows shipped, the one no-ship is #48 REJECTING a brief I priced off a filmstrip artefact (the worker was right), 0 reverts, selfVerdict == verdict throughout, $4.25/iter and 8 min with cost rising WITH output; the repeat fault is mine again: budget OVER (49.1/46 KB) and state.json five iterations stale because five workers ran with no manager pass between them. HELD RUNG 2: every climb from #32–#45 still has a compounding move, and #51 exposed the exact one — the going-home mechanism works and has no supply (6–9 agents after dark, under one leaver a night). b50 the bet: a night ARRIVAL source on its own budget (lane, 5 back, high risk, carries c85/c87); b51 the wind onto the river (river, 6 back); b52 the sill names a lit window and its cause (sill, 9 back, the observer has no night); b53 c81, drawGround 9.2 ms and ~650 rebuilds/day off the wear line; b54 the two instrument lies that cost #48 (filmstrip rate-as-pop, run-loop MANAGER_GAP). Curation: coverage #48–#52 entered; 8 cues resolved (6 promoted/folded, 2 closed), 4 held, 1 new (c89 church has no night); ledger 8 -> 5 with #50–#52 archived verbatim then condensed; 5 laws promoted paid for by compressing the inherited section — 30 laws, 11.8/12 KB; inventory trimmed to caps; WATCH night and wind notes rewritten to what is true after #50/#51. Budget 49.1 -> OK.
